from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A machine learing project to detect the faults in semiconductor wafer.',
    author='Pushkar Raj',
    license='MIT',
)
